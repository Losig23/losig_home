services:
  - type: web
    name: losig-home
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: DATABASE_URL
        value: sqlite:///losig_home.db
